{
  "name": "<%= appname %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "private": true,
  "author": {
    "name": "<%= user.git.name() %>",
    "email": "<%= user.git.email() %>"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "keywords": [],
  "dependencies": {},
  "devDependencies": {<% for (var i = 0; i < devDependencies.length; i++) {%>
    "<%= devDependencies[i].name %>": "<%= devDependencies[i].version %>"<%if (i < (devDependencies.length - 1)) { %>,<% } %>
  <% } %>}
}
