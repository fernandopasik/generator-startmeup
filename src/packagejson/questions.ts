interface Info {
  name: string;
  description?: string;
  private?: boolean;
  license?: string;
  authorName?: string;
  authorEmail?: string;
  authorUrl?: string;
  githubUsername?: string;
  repository?: string;
}
